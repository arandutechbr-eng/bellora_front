# Local (copie para .env — não commitar)
VITE_API_URL=http://localhost:8000/api/v1

# Vercel (recomendado — também funciona sem isso: build de produção usa Render por padrão)
# VITE_API_URL=https://zola-back.onrender.com/api/v1
